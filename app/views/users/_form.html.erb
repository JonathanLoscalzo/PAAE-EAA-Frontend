<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div class="card-panel purple lighten-2">
      <h5><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h5>

      <ul class="collection">
      <% @user.errors.full_messages.each do |message| %>
        <li class="collection-item"><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br>
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <br>
  <div class="field">
    <%= f.label :rol %><br>

    <%= f.select(:role, @roles, {}, { :class =>"browser-default"}) %>
  </div>

   <br>
  <br>
  <button class="btn waves-effect waves-light" type="submit" name="action">Crear
    <i class="material-icons right">send</i>
  </button>
<% end %>
